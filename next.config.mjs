/**
   * @type {import('next').NextConfig}
   */
 const nextConfig   = {
  // basePath:'/portfolio',
  images: {
    loader: 'akamai',
    path: '',
  },
  assetPrefix: './',
};

export default   nextConfig;